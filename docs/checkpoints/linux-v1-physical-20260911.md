# Linux V1 physical acceptance — in progress

## Latest checkpoint — 2026-09-11 17:40 America/Chicago

Native shared shell/package source is `d6e5230`, tag
`secure-client-linux-v1-native-rc1-20260911`. Both hosts passed native Portal,
Build, Inventory, persistent Back/Home, default validated HTTPS, API and secure
Socket.IO in the real GTK/WebKitGTK process. Renny also confirmed the visible
native shell. No debugging listener was added.

Both hosts passed managed-user negative probes for Tailscale, RFC1918, the
unrelated staging peer and public Internet; ordinary host Internet remained
available and private endpoint access remained denied. The real WebKit network
processes share the managed namespace with zero effective capabilities and
NoNewPrivs=1; renderers are further sandboxed, not in the host namespace.

Both passed ordinary packet-loss/recovery with the same Build document marker,
API and WSS restored, unchanged service PID, and the temporary namespace-only
drop table removed. An earlier administrative TAP-down probe was not counted:
it removed Mint's TAP and required restoring the test uplink. No VPN service
kill/restart scenario was used for the accepted packet-loss result. Two fixture
setup failures (not client results) were preserved rather than counted as PASS.

Central revocation deleted both generation-2 peers and both managed-user TCP
probes confirmed access loss. OLECLANKY staging peer remained connected.
Explicit re-provision then generated generation 3 on the same two records:

| Platform | Generation-3 artifact | SHA-256 | New peer / address |
| --- | --- | --- | --- |
| Mint | `cmtxj6gea0001o180yxjrj6kx` | `6273a15496a4490d1068791a7b2573e8f5fef239bb95f3a57d565bfa8cbfec49` | `dai83gbl0ubs7386jtt0` / `100.83.76.161` |
| Nobara | `cmtxj6y6y0003o180mjcbmyzv` | `c8572b5f7e9470d99fb49237b8974393f75ddd1be49738cb6b166b12f98e5543` | `dai83gafadhs73abi7dg` / `100.83.178.120` |

Both generation-3 keys report used_times=1, usage_limit=1, valid=false.
Both native package updates completed; remove/reinstall removed the launcher,
shell and namespace, retained enrolled state, and restored private DNS/HTTPS
plus the same negative checks. Immediate post-service-start diagnostics raced
DNS readiness and were not treated as client failures; subsequent real managed
probes passed. RPM reports the intentionally consumed bootstrap files missing
on removal. A Debian warning concerned test-generated bytecode, not an installed
client executable; test residue must be accounted for in final cleanup.

The four shared runtime files are byte-identical across both installed packages:

- namespace.py: `6b246b7b5c61f11600c13228952bcd131d3ea39e0c04dba283cd28d4591aaefa`
- runtime.py: `a66eeb953f90565c094d939634883339031f35024671190450333819548b6d07`
- shell.py: `91f5763f53be39ccdd2553df9eb58deb64373303b343d517cda7b656da7a4d35`
- shell_policy.py: `173cd6289699262ddd11586827263952de0b7f619011cd4e444edf9be85f9bd5`

**Not yet complete:** physical reboot checks (save-work confirmation requested),
then the once-only Windows/Android regression pass, one independent review,
bounded remediation if needed, final disposable-peer/test-service/database
cleanup, normal PR merges, V1 freeze and V2 re-entry brief. No freeze is claimed.

The following sections retain earlier provenance and are superseded by this
latest checkpoint where they describe pending work or generation-2 status.

This is not a PASS or V1 freeze record. Android PR #2 is already merged as
`e315f52d90921d09a9568590fadc1fc286047153`; issue #3 remains deferred.

## Candidate and scope

- Shared client candidate: `1afebd3`, tag
  `secure-client-linux-v1-candidate-3-20260911`.
- Isolated provisioner source: `0d06b84` (Stack main base `395ff6f`).
- Namespace baseline physically passed on Ubuntu ww-devbox and Nobara OLECLANKY:
  root transport HTTPS works; unprivileged public HTTPS blocked; host route/DNS
  unchanged. These preliminary probes are not the full client acceptance.
- Final Debian-family target is Cometforge / Mint 22.2, to avoid rebooting the
  ww-devbox control workstation. Fedora-family target is OLECLANKY / Nobara 44.
- OLECLANKY IP `192.168.50.1` was explicitly confirmed by Renny as its 10G
  development link. It must not be confused with its existing staging peer.

## Exactly two disposable device records

| Platform | Device record | Current artifact | Artifact SHA-256 |
| --- | --- | --- | --- |
| Debian/Mint | `cmtxfjb120000o1jtelop86g9` | `cmtxg2zp40001o1pow3re0hlh` (g2) | `bd034b2710e288d5836271e2078a83ed4d28ed50c2ae5487a1781af991130bb1` |
| Fedora/Nobara | `cmtxfjb1j0001o1jtqpea2yes` | `cmtxg3hkg0003o1poa0up0391` (g2) | `4dfc468e9efb5003ce540207759d3d96b880b9d382309c4164f349209a4c7806` |

The first candidate's settings lock was applied before enrollment. The exact
native error categories were SETTINGS / CONFIG / DISABLED; management HTTPS
inside Mint's namespace worked and the key remained unused. The shared code
now follows Windows' ordering: enroll, persist identity, then lock settings.
Both original unused installers were revoked through the existing service
before explicit re-provision of these same records. No extra records were made.

Mint generation 2 installed automatically with zero key entry and reports
ENROLLED/connected through real reconciliation. Peer:
`dai6qtrl0ubs73faice0`, IPv4 `100.83.51.15`.
The one-use key reports used_times=1, usage_limit=1, valid=false. Bootstrap files
are absent. The pinned installed transport hash is
`4eb4d7a2f5fe1a224362c68f6c8129502247635711db1970cc85ed921896780d`.
Host routes and `/etc/resolv.conf` match the before-state.

Preserved staging peer: `dabi9u2fadhs73a9u22g`, name `OleClankyV2`,
IPv4 `100.83.246.85`, connected. Its host NetBird remains 0.78.1; the independent
client bundles 0.77.1 without replacing that installation or its state.

## Temporary server instance — must be cleaned before completion

Explicitly authorized isolated instance on iws-prod:

- Service: `iws-linux-v1-test.service`, runtime unit under `/run/systemd/system`.
- Listener: **127.0.0.1:4601 only** (verified).
- Root: `/srv/iws-provisioning/linux-v1-test`.
- Separate empty database and role: `iws_linux_v1_test` in the existing
  provisioning PostgreSQL container. No production/shop tables were used.
- Existing production PAT is used in place through a private read-only mount;
  it must never be deleted/copied/rotated during cleanup.
- RPM tooling is extracted only inside this temporary directory; no production
  RPM packages were installed. Production Devices remains active and unchanged.
- Cleanup must revoke the two disposable peers and their current keys/artifacts,
  stop/remove only this test unit, drop only this test DB/role, and remove the
  temporary test state/downloads. Verify afterward and preserve nonsecret IDs,
  hashes and acceptance evidence locally first.

## Remaining gates

Managed browser Portal/API/WSS, complete native negative checks and ordinary
host browser behavior; Nobara automatic enrollment; reboot and ordinary loss/
recovery on both; revoke/access loss; explicit re-provision/new identity; final
disposable peer and server cleanup; one relevant Windows/Android automated
regression pass; one review/at most one remediation/scoped re-review; freeze.

No forced VPN-service-kill test. No V2 work before the freeze.

## Unrelated host issue, not repaired

Cometforge's package manager already had an inconsistent `chatgpt` package.
Only the two explicit IWS prerequisites were installed with targeted dpkg
operations and unrelated triggers deferred. That unrelated package was not
repaired, removed or reconfigured. Track host maintenance separately.
